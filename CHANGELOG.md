# شرح التغييرات في المشروع

## 1. تصحيح منطق استخلاص البيانات (Web Scraping)

- **الملف:** `lib/web_scriping/data_services.dart`
- **المشكلة:** كانت المحددات (selectors) المستخدمة لاستخلاص البيانات من HTML غير صحيحة ولا تتوافق مع هيكل الموقع.
- **الحل:** تم تحديث المحددات لاستهداف العناصر الصحيحة:
  - `tr.forum_post` للحصول على كل منشور.
  - `td.avatar-td img` للحصول على رابط الصورة.
  - `h3.details-head a` للحصول على عنوان الخدمة.
  - `ul.details-list li` للحصول على تفاصيل الخدمة.
- **النتيجة:** أصبح التطبيق يستخلص البيانات بشكل صحيح من الموقع.

## 2. إصلاح عرض البيانات في الواجهة الرسومية

- **الملف:** `lib/web_scriping/data_services.dart`
- **المشكلة:**
  1. `ListView.builder` لم يكن يُرجع `ListTile`، مما يؤدي إلى قائمة فارغة.
  2. كان يتم استخدام `Image.memory` بدلاً من `Image.network` لتحميل الصور من الإنترنت.
- **الحل:**
  1. تم تعديل `ListView.builder` ليُرجع `ListTile` بشكل صحيح.
  2. تم استبدال `Image.memory` بـ `Image.network` لعرض الصور من الروابط.
- **النتيجة:** أصبحت البيانات تُعرض بشكل صحيح في القائمة.

## 3. تحسين نموذج البيانات

- **الملف:** `lib/data.dart`
- **المشكلة:** كانت أنواع البيانات في `DataList` غير محددة (dynamic).
- **الحل:** تم تحديد أنواع البيانات `name`, `image`, و `description` إلى `String` لتحسين أمان الكود.
- **النتيجة:** أصبح نموذج البيانات أكثر قوة وأقل عرضة للأخطاء.

## 4. تبسيط الكود الرئيسي

- **الملف:** `lib/main.dart`
- **المشكلة:** كان الملف يحتوي على `StatefulWidget` غير ضرورية.
- **الحل:** تم استبدال `MainPage` (StatefulWidget) بـ `MyApp` (StatelessWidget) لتبسيط الكود.
- **النتيجة:** أصبح الكود أكثر نظافة وكفاءة.

## 5. تحديث الاختبارات

- **الملف:** `test/widget_test.dart`
- **المشكلة:** كانت الاختبارات لا تزال تستخدم `MainPage` القديمة، مما تسبب في فشلها.
- **الحل:** تم تحديث ملف الاختبارات لاستخدام `MyApp` والتحقق من عرض `CircularProgressIndicator` أثناء تحميل البيانات.
- **النتيجة:** أصبحت الاختبارات متوافقة مع التغييرات الجديدة.

## 6. إضافة ميزة "السحب للتحديث" (Pull to Refresh)

- **الملف:** `lib/web_scriping/data_services.dart`
- **المشكلة:** لم تكن هناك طريقة لتحديث البيانات بعد تحميلها لأول مرة.
- **الحل:**
  1. تم تعديل `WebScreping` لجعل قائمة الخدمات متغيرة محلية لضمان جلب بيانات جديدة في كل مرة.
  2. تم إضافة `RefreshIndicator` إلى واجهة المستخدم.
  3. تم تحديث الحالة (state) عند السحب للتحديث لإعادة تحميل البيانات.
- **النتيجة:** يمكن للمستخدم الآن سحب الشاشة لأسفل لتحديث البيانات.
